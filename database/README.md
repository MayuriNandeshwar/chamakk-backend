# Chamakk – PostgreSQL Database Setup (Development)

This folder contains the complete PostgreSQL database schema and seed data
required to run the Chamakk backend and admin panel locally.

---

## Requirements

- PostgreSQL 16+
- `psql` available in PATH
- Database user with create privileges (e.g. `postgres`)

---

## Step 1: Create Database

```sql
CREATE DATABASE chamakk_db;
```
